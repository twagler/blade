//#include "mower.h"

//void Print(GPS gps) {

// pc.printf("Lat: %.6f Long: %.6f Sats: %d\r\n", gps.latitude(), gps.longitude(), gps.numOfSats());
/*
   1. Prints current Latitude, Longitude, Number of satellites
   2. Prints Latitude and Longitude of next waypoint
   3. Prints values for the Ultrasonic sensors
   4. Prints left motor speed, right motor speed
   5. Prints the distance to the next waypoint
   
   */
// std::cout << "Current Location: " << gps.getLatitude(); << "," << gps.getLongitude();
//   cout << "Travelling to: " << LATwaypoint[incwaypoint(1)] << "," << LONwaypoint[incwaypoint(1)];

//printf("1,%f,%f,%d\r\n", gps.latitude(), gps.longitude(), gps.numOfSats());
//printf("2,%f,%f\r\n", LATwaypoint[incwaypoint(1)], LONwaypoint[incwaypoint(1)]);
//printf("3,%d,%d,%d,%d,%d\r\n", uSonic[0], uSonic[1], uSonic[2], uSonic[3], uSonic[4]);
//printf("4,%d,%d\r\n", leftspeed, rightspeed);

//}
